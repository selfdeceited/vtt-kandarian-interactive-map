# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Env files (contain tokens)
.env.local
.env.*.local

# Yarn Berry - release file is committed but cache is not
.yarn/cache
.yarn/install-state.gz

# Editor / OS
.DS_Store
*.local
